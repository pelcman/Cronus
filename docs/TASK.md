# TASK.md — サーバー/クライアントを「完成」させるまでの作業フロー

> **これがマスタープラン。** 作業はこのファイルのチェックリストに沿って進め、
> 完了のたびにチェックを更新してコミットに含める。経緯・履歴は [AGENTS.md](../AGENTS.md)
> (時系列ログ)、NPC個別の進捗は [NPC_COVERAGE.md](NPC_COVERAGE.md)(生成物)に置く。
> このファイルは「今なにをやるべきか」だけを持つ。

**「完成」の定義**: 身内が固定IPに接続して遊べるサーバーを、ドキュメントだけを頼りに
誰でも立てられる状態。①クライアントが落ちない ②主要コンテンツが機能する
③セットアップが `setup.bat` → `run-server.bat` → `port_open.bat` で完結する。

---

## 0. 軸 — 進め方の方法論(全フェーズ共通)

過去の重大バグ(カード効果ID・クエスト完了E3・攻撃ミラーのnSkillID)の反省から、
実装は必ずこの順で行う:

1. **逐語移植** — パケットの中身・送信タイミングは自作しない。リファレンス
   (Reference/JMSv186 の `ReqC*` → 送信呼び出し)の該当箇所を読み、呼び出し構造ごと
   移植する。「たぶんこう」で書いた箇所が全クラッシュの原因だった。
2. **契約監査** — 「リファレンスが送らない場面で送っていないか / 送る場面で
   送り忘れていないか」を机上で突き合わせる(フェーズ1)。
3. **差分検証** — 解析で決まらないものだけ検証に回す。ラウンドトリップテスト(自動)、
   golden vector(`CRONUS_BOT_CAPTURE` でJavaオラクルと比較)、実機二分探索(最終手段)。
4. **実機確認** — クライアント状態を壊しうる変更(ダイアログ・クエスト・インベントリ・
   フィールド遷移・エフェクト)は実クライアントで一度は踏む。

**解析だけで完結しない理由**(必ず覚えておく):
- リポジトリ自体が**v186では未検証の箇所がある**(Riremito氏いわく147/187が最良。
  カード効果ID=16はリポジトリのどこにも無く、実機二分探索でのみ判明)
- **多バージョン分岐**(v131〜302同居)の読み違えはコンパイラが検出しない
- **転記ミス**は自分では気付けない(完了時E3事件)

**オラクルの使い分け**:
| 質問 | 使うもの |
|---|---|
| パケットのバイト配置は? | `ResC*`/`Data*` クラス(逐語移植) |
| いつ・何を送る? | `ReqC*` ハンドラの呼び出し構造 |
| enum/効果IDの値は? | `Ops*` の `init()` のJMS186分岐。**無ければ疑う**(宣言デフォルトはGMS v95) |
| データの意味は? | gamedata.db(=クライアントと同一) + Java側 `Wz*` ローダ |
| どうしても決まらない | Javaオラクル起動+bot capture / 実機 `/booktest` 式二分探索 |

---

## フェーズ1: パケット契約監査(最優先・進行中)

今回型のクラッシュ(送信タイミング契約のズレ)を机上で先取りする。
リファレンスの各ハンドラとうちの `ChannelHandler.*` を突き合わせ、
**送る/送らない/順序** の差を全部洗う。クライアント状態を壊しやすい系から。

- [x] クエスト (`UserQuestRequest`) — 完了時E3は nextQuest 時のみ・記録が先 (e1cdb92)
- [x] NPC会話 (`UserSelectNpc` / `UserScriptMessageAnswer` / ScriptMessage の全型)
      — 監査結果: 応答デコード(menu=int/text=str/avatar=byte/slide=int・ESC意味論)は一致。
      **修正2件**: ①ショップ/倉庫を開いている間のNPCクリックを拒否(oracleの会話ロック相当)
      ②マップ移動時に shop/storage/会話状態をクリア(ロック残留防止)。
      **休眠ギャップ**(未使用のため未修正・使う時に直す): SM_SAYIMAGE のcountバイト、
      SM_ASKNUMBER/QUIZ系のペイロード(現行スクリプトAPIに無い)
- [x] **NPC会話の選択肢ID検証**(2026-09-07, Riremitoさんの指摘) — jms_scripts のタクシー系は
      `#L<mapid>#` の選択値をそのまま `warp` に渡す exploit を意図的に残している(「ちゃんと
      選択肢IDを振らないとダメ」)。Cronusはエンジン側で一括解決: `askMenu` が提示した `#L?#` の
      集合を記憶し、集合外の応答は `Rejected` として会話終了(スクリプトには届かない、`[npc]` ログ)、
      `askAvatar` も候補範囲外を拒否。あわせて「メニューを閉じた(action≠1)」は会話終了、
      無応答タイムアウト後は stale な選択で続行せず終了。ワイヤテスト(未提示IDで warp されない)で回帰固定。
- [x] フィールド遷移 (`UserTransferFieldRequest` / `UserPortalScriptRequest` / 死亡復帰)
      — 監査結果: 失敗時 `TransferFieldReqIgnored` は転送要求側では実装済みだった。
      **修正2件**: ①ポータルスクリプト要求の失敗(未知ポータル/スクリプト無し/エンジン無効)が
      無応答だった → 拒否パケットで排他ロックを解除 ②死亡復帰の `revive_type` を解釈
      (>0 = その場復活、0 = 帰還マップ。HP/MP全快は一致済み)。
      在マップテレポート(`PortalTeleportRequest`)はoracle自体が not coded — 未対応でOK。
      MigrateIn/チャンネル移動は実機で常用済みのため対象外とした。
- [x] インベントリ 排他ロック解除の全パス監査 — oracleの規則「失敗も必ず `updateInv()`
      (空InventoryOperation) か StatChanged で応答」に対し、**沈黙分岐11箇所を修正**:
      アイテム使用(死亡中/desync/帰還先なし)、スクロール(desync/回数切れ/対象違い)、
      スロット移動(装備間移動拒否/空スロット)、地面ドロップ(空スロット)、
      拾得(死亡中/取得済み)。バフ解除は oracle 同様 **常に** TemporaryStatReset を返すよう変更。
      機械走査ツール: 早期returnの直前5行に応答packetが無い分岐を列挙する方式(再利用可)。
      残り(InventoryOperation 全モードのバイト検証・Gather/Sort応答)は golden vector 拡張時に。
- [x] ショップ (`UserShopRequest` 各op) — v186表(Close=4・結果0-12)は一致、
      buy/sell/rechargeの失敗コードも一致。**バグ修正1件**: リチャージ品(手裏剣・弾)の購入は
      oracleでは「定額でスタック満タン」なのに価格×個数・個数分付与になっていた。
      **軽微修正1件**: 補充要求が既に満タンの場合に無応答だった → RechargeUnknown で応答
      (oracleは未定義enumを送る箇所のため、定義済みコードを選ぶ意図的な差)。
      ペット購入のペットインスタンス生成は未対応(NPC店でのペット販売なし、キャッシュ側は対応済)。
- [x] 倉庫(Trunk) — 預入/引出/メルの失敗は TrunkError(PutNoMoney/IncorrectRequest/NoSpace等)で
      全分岐応答済みを確認。ショップ/倉庫とも「開いていない状態の要求は沈黙」も oracle と一致。
- [x] トレード・ミニゲーム・個人商店(ルーム系) — 入室失敗は全経路 MiniGameFull で応答済み。
      ルーム内操作(put/buy等)の失敗は **oracle自身がほぼ沈黙**(ReqCMiniRoomBaseDlg)であり、
      うちの沈黙分岐は契約一致と判定。差分なし。
- [x] スキル・バフ — ackが先(SkillUseResult)・バフ順・cooldown・パーティバフは一致。
      **修正3件**: ①未習得/効果なしスキルのキャスト後に StatChanged 空アンロックを送る
      (oracleの sendStatChanged(true) 相当) ②スキルキャンセルの周囲通知 `LP_UserSkillCancel`
      を新規実装(未送信だったため他人視点でオーラが残っていた)③ポータルスクリプト起動前の
      効果音 PlayPortalSE=10 を送出(v302表−1の法則、**実機検証済み 2026-08-26**)。
      残メモ: MP不足でもバフが掛かる(サーバー権威の締め付けはフェーズ4で)。
- [x] 戦闘・被弾 — 攻撃ミラー(nSkillID/keydown/メソ爆発)は修正済み。今回:
      **被弾ミラー `LP_UserHit` が未実装だった**のを発見・移植(oracleは全被弾分岐で
      無条件ブロードキャスト — 他人の被ダメ数字・仰け反りが今まで一切見えていなかった)。
      CP_UserHit のフルデコード(v186 攻撃index表: Magic=0/Physical=-1/Obstacle=-2/
      Stat=-3/Counter=-1000、Mob攻撃ブロック、障害物short)も併せて移植。MISS(0)も
      ミラーする。fake-skill(-1)形はレジストリ未保持のため未対応(oracleもhack扱い)。
      被弾HP適用・死亡ペナルティ・パーティ通知は既存どおり。ミラーはテストでピン留め。
      残: deadlyAttack/mpBurn/mob状態異常スキル適用はフェーズ4(状態異常系)とセット。
- [x] ソーシャル — パーティは失敗を PartyResultSimple のエラーコードで全応答済み、
      バディも FriendSet* コード実装済み、ギルドのUI可視失敗(本部要件/満員/メル不足)は
      通知で応答済みを確認。沈黙分岐は権限/整合性ガード(正規クライアントは送らない)のみ。
      **修正2件**: ①キャッシュアイテム使用(拡声器・黒板)の失敗3経路が無応答だった →
      oracleのdispatcher契約(`!handled → updateInv`)どおり空InventoryOperationで解除
      ②ペットフードのdesync失敗も同様に解除。
- [x] キャッシュショップ — 入退場はbot往復で実証済み、購入/ロッカー移動の失敗は
      FailResult(NoStock/NoRemainCash/NoEmptyPos)で全応答済みを確認。差分なし。

**フェーズ1完了(2026-08-25)**: 10系統の監査で修正21件+リチャージ購入バグ+被弾ミラー移植。
今後の新ハンドラは「軸」の手順(逐語移植→契約確認)を踏むこと。

**やり方**: 系ごとに (1) リファレンスの送信呼び出しを列挙 → (2) うちの同じパスの送信を列挙 →
(3) 差分を表にして判定(意図的な差か・バグか)→ (4) 修正+テスト → (5) この表にチェック。
発見した差分は監査メモとしてコミットメッセージに残す。

## フェーズ2: NPCカバレッジ(継続作業)

[NPC_COVERAGE.md](NPC_COVERAGE.md) 基準(現在: 1,447体中 800対応 = 55%; none 647)。
`python DevTools/npc_coverage.py` で再生成。

- [x] 無反応クリックの廃止(汎用フォールバック行)
- [x] ガシャポン15台(チケット/1万メル)
- [x] ネオトウキョウ開通(クリスタル=時代ワープ)
- [~] ビクトリア各町の `none` NPC(進行中 — 2026-08-25 第1弾で9体実装):
      精錬4体(ビシャス/サンダー/スミス/セリル: 原料10+手数料→精錬品、全ID実データ検証済み)、
      ピル(リス港→各町ワープ)、カイジ+チコ(ミニゲーム案内+セット販売)、
      ルイス(忍耐の森出口)、クロイ(ペット案内)。レシピ/価格はオラクル欠落のため簡易(創作)と
      スクリプト内に明記。残: `enter_warrior/magicion`(ヘンケル)、草むら/イベント系、
      ドラゴン/エヴァン系(`dragon_dream`等 — エヴァン未実装のため保留)
- [~] オシリア(進行中 — 2026-08-26 第1弾で交通4体):
      危険地域弾丸タクシー、イルカ(いずれも2026-09-07に本来の行き先へ修正、下記)、
      オルビス塔の魔法石×2(1層⇔20層の近道)。行き先マップIDは実データ検証済み、
      料金は簡易(創作)。第2弾(同日)でホーンテイル入口群6体(道標=迷路入場、木の根の水晶=**迷路脱出**、
      切り株=抜け道、遠征隊の標識=試験の洞窟入場、石版・ケロベン=案内)と
      ザクムガイド3体+聖なる岩+道場掲示板(フレーバー)を追加。
      残: make_*(オルビス/ルディ製作 — レシピのオラクル無し)、ludi/earth系ギミック石、
      天空の扉(SkyGate)、エオス石
- [x] **移動系NPCの行き先範囲を本来どおりに復元**(2026-09-07) — 「停留所案内員=世界移動
      直行便」として汎用化していた31体を、wz `info/script` 名と Riremito/jms_scripts(JMS原文)で
      役割ごとに戻した: タクシー7体=ビクトリア6町限定(初心者1/10・料金は創作)、高級タクシー2体=
      アリの巣広場1万メル(原文)、フロリナ送迎3体(2000メル/フリーパス4031134は消費しない)+
      パイソン(乗船元へ戻る)、カニング地下鉄(ウンイ切符・改札口・出口=新規3体、B1〜B3は入場券制)、
      エレヴ/リエン船6体=直行ワープ(原文の料金1000/800)、オルビスハブ(イフ=4路線チケット、
      イス=昇降場案内、リニ=エリニア行き乗船→控え室、スナ/ラミニ/ゼラス+ティティアン/タミ/
      アセソン=チケット引換で直行、モル/ミュ/シラス=チケット販売、控え室出口7体=返金なし)、
      危険地域弾丸タクシー=町ごとに1か所(原文)、イルカ=四角地帯(チケット4031242)/白草村1万メル。
      ジョエルの直行便メニューも撤去。エリジャー(2012026)は wz に script 指定の無いクエストNPCの
      ため汎用スクリプトを削除。オラクルに値が無い料金は全て創作としてスクリプト先頭に明記。
- [ ] ジパング/自由市場/イベントマップ同上
- [x] 宅配 `parcel` NPC(ドイ) — 殻実装済み(フェーズ3参照)。倉庫 `trunk` NPCは Goldman 系スクリプト済み
- [ ] 完了基準: スポーンNPCの `none` を10%未満に(残りは純フレーバー)

## フェーズ3: 未実装機能(コンテンツ系)

- [x] **宅配(parcel)** — 本実装完了(2026-08-26)。SENDペイロードは実機ワイヤキャプチャ
      2回の差分から解読(`[tab:1][slot:2][qty:2][meso:4][宛先:str][flag:1]`)。
      発送: 検証(宛先存在0x0E/同一アカウント0x0F/メル不足0x0C/desync 0x0D)→アイテム・メル徴収
      →parcelsテーブルへ永続化(自動マイグレーション確認済み)→0x13。装備は実体ごと移送。
      受取: ドイの窓口で手渡し(受信UIのバイト仕様が未解明のため — 空き不足分は保留)。
      キャプチャバイトそのものを回帰テストにピン留め。**実機での送→受テスト待ち**。
      補足: ポータルSE=10は実機検証済み(2026-08-26 ログで生存確認)。
- [ ] **モンスターカーニバル / パーティクエスト**(カニングPQ等) — FieldSet.img が
      オラクル欠落(BMS Server.wz)のため、実装は簡易版から
- [x] **飛行船(エリニア⇔オルビス)**(2026-09-03) — 駅/機内での `CP_CONTISTATE` 応答は
      oracle(`ReqCField.OnContiState`)逐語: 駅9箇所=停泊(`CONTI_WAIT`)、機内2箇所=
      `TARGET_MOVEFIELD+MOBGEN`(+クリムゾンバルログ9300210を機内が空なら1体湧かせる=創作)。
      運航は **oracle欠落(Continent.img無し)のため自作**: 壁時計15分周期(乗船10分/飛行5分)、
      `AirshipService` が控え室→機内→到着駅を各乗客の `FieldPlayer.WarpAsync`(自セッションの
      MovePlayerToMapAsync — NPCスクリプトのwarpと同経路)で移動。切符4031045/4031047は
      ジョエル/イフが販売、チェリ/イスが乗船、プリンが控え室案内。実機で乗船→出発→機内を確認済み。
      **修正(同日)**: 初版は機内入場の `CP_CONTISTATE` に oracle どおり `MOBGEN` を返し独自に
      バルログを即湧きさせていた(oracleは時系列を持たない)→ 敵船タイムラインを時刻から決定的に
      算出する方式へ: 入場=`CONTI_MOVE`(3)、飛行+60秒=敵船到着 `MOBGEN`(4)+クリムゾンバルログ
      `AirshipBalrogCount`体を先頭乗客の足元に、到着30秒前=`MOBDESTROY`(5)+残りを消去
      (`Field.RemoveMob` 新設)。周期ごとの出現可否は `AirshipBalrogChance`(既定1.0=毎回)を
      周期番号シードで決める(ハンドラとtickが共有状態なしで一致)。`CONTI_MOVE/MOBDESTROY`
      の値は enum 宣言値(v186 initは空)で**実機未検証** — 次の乗船で要確認。
      (出発/到着演出 `CONTI_START/END` は引き続き未送信)。
      **修正2(2026-09-07, 実機報告3件)**: ①敵船の出現演出が無い — ワイヤログで `CONTIMOVE(10,3)`→
      `(10,4)` が届いているのに船が描かれないことを確認(再入場時の `(10,4)` 単発も同様)。oracle の
      `(10,4)` は**演出を出さない**と判明。候補として駅の停泊表示と同じ `LP_CONTISTATE` に
      `MOBGEN(4)`+AppearShip=1 を送る方式に切替(oracle の CONTIMOVE も併送)、入場時の `(10,3)`
      は撤去。**実機未検証** — 出なければ `/conti state|move <a> <b>` で機内(200090010)から
      候補を手送りして特定する(候補: `state 4 1` / `state 4 0` / `move 4 10` / `move 8 4`)。
      ②湧く敵が道場版クリムゾンバルログ(9300210: 歩行、revive=透明チェックMob 9300216)だった →
      本来の飛行Mob **レッサーバルログ 8150000**(flySpeed 10、GMS名 Crimson Balrog)に変更し、
      出現位置を乗客の足元から**敵船オブジェクト(`shipObj` 485,-221)の上空**へ。
      ③駅の電光掲示板が00:00固定 → 駅マップ(wz トップレベル `clock` ノード、17マップ)への入場時に
      `LP_Clock` type1(hh:mm:ss、oracle `TacosMap.addPlayer` 逐語)でサーバー機のローカル時刻を送信。
      **修正3(同日, `/conti` 全候補で敵船が出ないとの報告)**: 原因はパケットではなく**クライアントの
      データ**。飛行マップが指す敵船画像 `Map/Obj/vehicle.img/ship/ossyria/97` は Map.wz 内で
      **1×1の空キャンバス**(乗客船 98/99 は 564×549 / 545×746)。GMS v83 系(HeavenMS `crogBoatPacket`)
      も oracle と同じ `CONTIMOVE(10,4/5)` でクリムゾンバルログ船を出しており、パケットは正しい。
      演出復元はクライアント側 Map.wz への画像パッチが必要(他バージョンの Map.wz から 97 を移植)。
      サーバーは oracle どおり `CONTIMOVE(10,4)/(10,5)` のみ送る形に戻した(`CONTISTATE(4,1)` 併送は撤去)。
      あわせて **レッサーバルログが動かない**件: 襲撃スポーンで操作権(`MobChangeController`)を誰にも
      渡しておらず、クライアントが `CP_MobMove` を一度も送っていなかった(ログで確認) → 先頭乗客に
      aggro 付きで操作権を渡す(MobRespawnService と同じ)。
- [ ] **ボス入場スクリプト**(`Go_boss*`・ザクム祭壇の完全フロー・パプニカ?)
- [ ] **イベントシェル** — 期限切れイベントNPCに「開催していません」以上の再現を
      するかは要相談(ビジターイベント等は創作になる)
- [ ] **ガシャポンのプール調整** — 実プール(BMS Gachapon.img)が入手できたら差し替え
- [ ] **結婚式場**(wedding マップ群) — 優先度低
- [ ] **モンスターブックの充実** — ドロップ由来カードの網羅確認
- [ ] **残り路線の実飛行化** — オルビス⇔ルディ/リプレ/アリアント、エレヴ⇔エリニア/オルビス、
      リス港⇔リエンは現在チケット引換の直行ワープ(Riremitoさん版と同じ)。控え室/航海中マップは
      wz に揃っている(200000122/132/152、200090100〜/200〜/400〜、200090020/030/060/070)ので
      `AirshipRoute.All` に路線を足し `HandleContiStateAsync` の機内マップ表を広げれば同じ
      スケジューラで運航できる。バルログ襲撃はエリニア⇔オルビスのみ。

## フェーズ4: ゲームシステム完全性(リファレンス比)

AGENTS.md の「Deferred」を再評価して潰す:

- [~] **Mob攻撃の特殊効果**(2026-09-03) — oracle `OnUserHit` の MobAttackInfo ブロックを逐語移植:
      **deadlyAttack**(表示は hp−1、HP/MPを1に)、**mpBurn**(MPのみ減少、HPは無傷)、
      **conMP**(攻撃ごとにMobのMP消費。Mobの MP を `MaxMp` でスポーン時に初期化する修正も同梱)。
      攻撃データは `attack{N}/info` から `MobAttackInfo` としてパース(`info/link` 追従、
      oracle `MobWz.getMobAttackInfo` どおり。実データ: deadly 51 / mpBurn 6 / disease 167 攻撃)。
      **状態異常(毒・封印・暗闇…)は未適用**: oracleの `MobSkill.applyEffect` はMob自身のバフのみで
      プレイヤーへの病気付与が**存在せず**、v186の病気形 TemporaryStatSet(上位マスク語+level/skill
      パッキング)のバイトオラクルも無い(カード効果ID型のクラッシュ地帯)。データ配管は済ませ
      `GameConstants.PlayerDiseasesEnabled=false` で封印。有効化には `/booktest` 式の実機二分探索が必要。
- [ ] **マスタリーブック**(現在: wz最大まで直接上げる簡易仕様) — 本仕様に寄せるか判断
- [ ] **ギルドBBS**(LP opcode がリファレンスでも未解決) — 保留のまま可
- [ ] **アライアンス** — 保留のまま可
- [ ] **ミニゲーム招待UI** — ルーム系監査とセットで
- [ ] **レート/経験値テーブルの最終確認**(QuestExpByLevel.img 等)
- [x] **初心者のLv10まで自動AP振り分け**(2026-08-26) — oracle `MapleCharacter.levelUp`:
      初心者職(0/1000/2000/2001/3000)は新レベル≤10のとき残APを全てSTRへ、SPは無し。
      未実装だった(APが貯まるだけ)→移植+テスト3本。残差: MP成長のINT/10は
      oracleが総INT(装備込み)、うちは基礎INT。
- [ ] **ダメージ計算の設計メモ**: 与ダメ・被ダメとも**クライアントが計算**し、サーバーは
      適用のみ(oracle `OnUserHit`/攻撃処理とも同じ。oracleは上限検証すらしない)。
      うちは任意の行上限のみ(2026-08-26: デフォルト無効、有効時50,000,000 — `GameConstants`
      / `CRONUS_DAMAGE_CAP_ENABLED` / `CRONUS_DAMAGE_CAP`)。装備・ステ・バフの反映はサーバーが送る
      CharacterData(golden vector検証済)/装備ステ(wz由来)/TemporaryStatSet(スキルX値)の
      正しさに依存。将来の締め付け候補: ステ由来の最大ダメ検証(oracle無し=創作になる)。
- [x] **`/gmmove`**(2026-09-07) — GM移動モード。サーバー側フラグ(被ダメージ無効・スキルMP消費/
      クールタイム無し)+一時ステータス Speed+200/Jump+23/**Flying**(CTS bit 80、JMS186の
      `OpsSecondaryStat.init` 準拠、理由=スキル1026 플라잉)。`LP_TemporaryStatSet/Reset` を128bit
      マスク対応に拡張(bit≥64は word[2]/[3])。
      **追補(同日, 実機報告)**: ①速度が140%で頭打ち → クライアント exe の clamp 6か所
      (`min(x,140)`×2, `min(x,123)`×3, 移動制御の上限比較×1)を逆アセンブルで特定し、条件ジャンプ
      2バイト書き換えで無効化する `DevTools/clientpatch_speedcap.py`(status/apply/revert、周辺バイト照合)
      を作成・適用済み。Jump は +80 に。②飛行しない → `CTS_Flying` は `info/fly=1` のマップ(72枚)でのみ
      有効と判明。`wz set-int` で全 5983 マップに fly=1 を立てる `DevTools/wz_enable_fly.bat` を用意
      (実 Map.wz で生成・verify 済み、差し替えはユーザー実行)。③MP/HP が減る → クライアント側の先行減算。
      スキル使用/スキル攻撃後にサーバー値の `StatChanged(Hp|Mp)` を返して打ち消す。
      **追補2(同日)**: 速度解除は実機で有効。`/gmmove <速度倍率> [<ジャンプ倍率>]` に拡張。
      **追補3(同日)**: 攻撃速度も解放 — クライアントはアニメ生成時に段階を [2,10] に丸め、
      フレーム時間 = 基準×(段階+10)/16(exe 4か所)。丸めを外し、`/gmmove` 第3引数の倍率から
      Booster 値(= 16/倍率−10 − 武器attackSpeed、段階−8が下限)を逆算して送る。武器の
      `attackSpeed` を `EquipStats` に追加。歩行アニメの再生速度上限140%→1000%(2か所)も解除。
      Flying は fly=1 マップでも効かず(要件不明)、ユーザー要望も「空中で再ジャンプ」なので撤去。
      空中ジャンプはクライアント物理(CVecCtrlUser)の接地判定の解除が必要: 物理定数テーブル
      (`[[0xD706D8]+8]`, jumpSpeed=+0x70)と、ジャンプ力×jumpSpeed を適用する更新関数(exe 0x70DDC6)
      までは特定済み。キー入力→ジャンプ許可の分岐は未特定(**継続課題**)。
      **追補4(同日, 実機報告)**: 12か所適用後もステータス窓が 140% / 123% のまま → 1次のサイトは
      リモートプレイヤー用の SecondaryStat と別経路の移動制御だった。`CUserLocal` の物理値関数
      (exe 0x687A80〜)を読み切り、「速度 = min(合計, 上限140 [即値] / 乗車時190)、ジャンプ =
      min(max(合計,80),123) → ×0.01 で移動制御へ」の3コピー(徒歩・乗車2経路)と、ステータス窓表示の
      元になる3つ目の SecondaryStat の min(x,140) 2組、他プレイヤー表示用の min(速度,140) を特定。
      計9か所を追加(合計21か所、即値 140/190→10000、`jl`→`jmp`)し適用済み。
      **追補5(同日, 実機報告)**: それでもステータス窓は 140% / 123% → 窓の描画関数(exe 0x5DAA32〜)が
      SecondaryStat の GetSpeed()/GetJump()(丸め無しの合計)を呼び、描画時に自前で min(140/190) と
      「>123 なら 123」を掛けていた。徒歩・乗り物・ペット乗車の3経路、6か所を追加(合計27か所)し適用済み。
      **追補6(同日, ユーザー報告)**: 変化が無い真因は**別フォルダ** — ショートカットは
      `Client\MapleStory_v186`(RunEmu.ini)を起動しており、exe 27か所と fly=1 の Map.wz は
      `Client\MapleStory_v186_edit` にだけ当たっていた(atime で確認)。DevTools の既定値を起動側の
      フォルダに変更し、ドキュメントの「_edit が遊ぶ側」を訂正。よって「Flying は fly=1 でも効かない」
      という結論も未検証に戻る → `/gmfly [on|off]` を `/gmmove` から独立した切替として実装(Flying bit 80
      のみ、OFF で Flying だけ Reset)。クライアント終了後に起動側フォルダへ exe 27か所と fly=1 Map.wz を
      適用済み(20:29-20:30、`.orig` / `Map.wz.bak` あり)。実機で `/gmmove 3 5 5` の表示と `/gmfly` の飛行を確認待ち。
      詳細は [CLIENT_PATCHES.md](CLIENT_PATCHES.md)。
- [x] **存在しないマップに保存されたキャラの救済**(2026-09-07) — `/warp` の打ち間違いで
      データの無いマップIDに飛ぶと、SetField でクライアントが落ち、再ログインでも同じ地点で落ちて
      復帰不能だった。対策2段: ① `/warp <id>` は Map.wz に無いIDを拒否(`IMapProvider.KnowsAllMaps`
      = 完全なデータセットのときだけ判定、テスト用の部分プロバイダは対象外)、② ログイン時に
      保存マップのデータが無ければ `GameConstants.RescueMapId`(既定 100000000 ヘネシス)へ移して
      保存してから SetField(創作: リファレンスは保存マップを信用する)。

## フェーズ5: 検証基盤

- [ ] **golden vector拡張** — 戦闘・ショップ・クエストの capture を採り、回帰テストとして
      ピン留め(現状: ログイン→入場のみ)
- [ ] **Java差分ハーネス自動化** — 同一bot操作を両サーバーに流してバイト差分を出す
      スクリプト(`CRONUS_BOT_CAPTURE` の拡張)
- [ ] **長時間ソーク** — 実クライアント放置+bot併走を数時間、メモリ/切断監視
- [x] **wz 編集環境**(2026-09-07) — `DevTools/wz.bat`(`DevTools/WzTool`, 実装は `src/Cronus.Data/Wz/`):
      .img のプロパティ木リーダー/ライター(文字列重複表・拡張ブロック・キャンバス/サウンド原文複製)、
      アーカイブ再構築(ヘッダ複製・ディレクトリ再直列化・未変更 img はバイト複製)、キャンバス復号→PNG、
      `graft`(別 wz からノード移植、鍵差は平文 zlib に正規化)、`verify`(全 img 解析比較)。
      **Map.wz 598MB を無変更で再構築 → 5983 img 全一致(0.9秒)** を確認。往復テスト7件。
      使い方と移植レシピ(飛行船バルログ船 / アバター)は [WZ_TOOLING.md](WZ_TOOLING.md)。
      バルログ船画像の移植は `DevTools/wz_graft_airship.bat <移植元 Map.wz>` — **移植元(97が実画像の
      Map.wz)がこのマシンに無いため未実施**。残: 新規 img の追加、PNG→キャンバス取り込み。
- [x] **クラッシュ報告の定型化** — `DevTools/wirelog.py`(2026-08-26)。切断ごとに直前の
      送受信をopcode名+要旨(クエストid/NPC/ポータル名/チャット等)付きで注釈表示。
      `--summary`(opcode頻度)、`--opcode XXXX`(単一opcode追跡)。4681クラッシュ解析を
      1コマンドで再現できることを確認済み。以後クラッシュ報告を受けたらまずこれを実行。

## フェーズ6: デプロイ(最終目標に直結)

- [x] **LAN IP広告リハーサル**(2026-09-03) — `CRONUS_HOST=192.168.1.80` で広告し、bot 4体を
      LAN IP経由で全99ステップ(チャンネル移動 `now on 192.168.1.80:7576`・キャッシュショップ往復)
      通過。広告IPの仕組みは健全。`.env` の `CRONUS_HOST` を公開IP `219.100.161.153`(=このPC)に
      設定し起動ログで確認済み(以前は 127.0.0.1 のままで「ログイン後の入場で自PCへ誘導→切断」になる
      状態だった)。
- [ ] **公開IPリハーサル(外部テスター)** — **残作業: Windowsファイアウォールに受信許可規則が無い**
      (`Get-NetFirewallRule` で Cronus/Maple 規則なし。8484/7575-7577 は 0.0.0.0 で待受中)。
      管理者権限で `port_open.bat` を実行+ルーターのポート転送後、外部から `Test-NetConnection`。
- [x] **プレイヤー配布手順(EmuClient)** — SERVER_SETUP.md のTODOを実証手順で置換(`LocalHost.ini` /
      `RunEmu.ini`、CRLF必須、TargetEXEはクライアントフォルダ内、CRONUS_HOST=公開IP必須)。
      スクリーンショット付き非技術者向け版は未作成。
- [ ] **Docker動作確認**(構造レビュー済み・実行未)
- [ ] **バックアップ運用の確認**(cronus.db コピーの実地テスト)

## フェーズ7: 拡張(完成後)

- [ ] 他バージョンクライアント対応(147/187) — WzArchive はバージョン自動検出済み。
      opcode表+Config 分岐の追加で成立するかをスパイクで確認
- [ ] 新バージョンwzからの部分移植(髪型1個の注入実験から)
- [ ] Serilog / EF Core migrations / Span\<byte\> パケットAPI(技術負債)
- [ ] Maple2型アーキ整理の続き(partial → Game 層への論理移動)

---

## 進行ルール

1. 着手順は **フェーズ1 → 2/3並行 → 4 → 5はフェーズ1〜4の中で随時 → 6**。
2. 1コミット=1まとまり。チェック更新はそのコミットに含める。
3. クライアントを落としうる変更は、コミット前にテスト+botスイート、コミット後に
   実機確認を依頼する(ユーザーに「◯◯を踏んでみて」と明示)。
4. 「オラクルに無い挙動」を創作した場合は、コードコメントとコミットに **創作である旨** を残す。
5. 週次目安で `npc_coverage.py` を再生成して数字を更新する。
