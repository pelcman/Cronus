// ドイ (宅配, wz info parcel=1) — ホームデリバリーの受付。
// 宅配UIの開閉はオラクル(ResCParcelDlg)どおり。発送処理そのものは送信ペイロードの
// バイト仕様が未解明のため未対応(送ろうとするとエラー表示でアイテムは保全される)。
function start() {
    var pick = cm.askMenu("こんにちは、宅配のドイです。"
        + "\r\n#L0#宅配を利用する#l"
        + "\r\n#L1#宅配って何?#l");
    if (pick == 0) {
        player.openParcel();
        return;
    }
    if (pick == 1) {
        cm.sendOk("宅配は他のキャラクターへアイテムやメルを送れるサービスですよ。"
            + "\r\n#bただいま発送機能は調整中#kです。窓口を開くことはできますので、ご容赦を…。");
    }
}
